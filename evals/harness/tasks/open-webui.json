{
  "repo": "open-webui",
  "portfolio": "test-repos/open-webui",
  "note": "Backend-focused (the Svelte frontend has no handler, so it is not in the graph). Gold verified by hand against the tree 2026-05-30. Larger/more complex than fastapi-template: pluggable vector-store factory, ~30 routers, PersistentConfig.",
  "tasks": [
    {
      "id": "q1-auth",
      "question": "How does the backend authenticate API requests? Find where bearer/JWT tokens are created and decoded, the dependency that resolves the current user from a request, and where the Auth record + sign-in/sign-up endpoints live. Name the exact files and key functions.",
      "gold": [
        "backend/open_webui/utils/auth.py",
        "backend/open_webui/models/auths.py",
        "backend/open_webui/routers/auths.py"
      ]
    },
    {
      "id": "q2-vector",
      "question": "The app supports many vector databases behind one interface. How is the active vector store selected at runtime, what is the common interface every backend implements, and where is the pgvector implementation? Name the factory, the type/interface, and the concrete file.",
      "gold": [
        "backend/open_webui/retrieval/vector/factory.py",
        "backend/open_webui/retrieval/vector/main.py",
        "backend/open_webui/retrieval/vector/type.py",
        "backend/open_webui/retrieval/vector/dbs/pgvector.py"
      ]
    },
    {
      "id": "q3-config",
      "question": "Configuration here is persisted and editable at runtime, not just read from env. How does the PersistentConfig mechanism work, where are the raw environment defaults defined, and which router exposes/updates config over the API? Name the exact files.",
      "gold": [
        "backend/open_webui/config.py",
        "backend/open_webui/env.py",
        "backend/open_webui/routers/configs.py"
      ]
    },
    {
      "id": "q4-chats-db",
      "question": "Where is the Chat data model defined, how is the database engine/session set up, and where does Alembic run migrations? Name the exact files and the key classes/functions.",
      "gold": [
        "backend/open_webui/models/chats.py",
        "backend/open_webui/internal/db.py",
        "backend/open_webui/migrations/env.py"
      ]
    },
    {
      "id": "q5-rag",
      "question": "Trace the retrieval (RAG) query path: which router handles retrieval/query requests, and which module holds the shared helper logic that embeds a query and searches the vector store? Name the exact files.",
      "gold": [
        "backend/open_webui/routers/retrieval.py",
        "backend/open_webui/retrieval/utils.py"
      ]
    }
  ]
}
